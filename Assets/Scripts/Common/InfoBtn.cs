using System;
using System.Collections;
using System.Collections.Generic;
using System.Xml;
using UnityEngine;
using UnityEngine.UI;

public class InfoBtn : MonoBehaviour
{
    Button btn;


    void Start()
    {
        //btn = GetComponent<Button>();
        //Image img = GetComponentInChildren<Image>();

        //if (img != null)
        //{
        //    img.sprite = UIShop.Instance.spriteBtnInfo;
        //    if (SaveManager.GameProgress.Current.GetStringValue("info_btns", transform.parent.name) == transform.parent.name)
        //        img.sprite = UIShop.Instance.spriteBtnInfoDefault;
        //}
        //if(btn != null)
        //{
        //    btn.onClick.AddListener(() =>
        //    {
        //        if (img != null)
        //            img.sprite = UIShop.Instance.spriteBtnInfoDefault;
        //        SaveManager.GameProgress.Current.SetString("info_btns", transform.parent.name);
        //        SaveManager.GameProgress.Current.Save();
        //    });
        //}
    }


}
